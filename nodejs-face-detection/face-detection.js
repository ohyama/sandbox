// Generated by CoffeeScript 1.9.1
(function() {
  var opencv;

  opencv = require('opencv');

  opencv.readImage("./sample.png", function(err, im) {
    return im.detectObject(opencv.FACE_CASCADE, {}, function(err, faces) {
      var i, j, ref, x;
      for (i = j = 0, ref = faces.length - 1; 0 <= ref ? j <= ref : j >= ref; i = 0 <= ref ? ++j : --j) {
        x = faces[i];
        im.ellipse(x.x + x.width / 2, x.y + x.height / 2, x.width / 2, x.height / 2);
      }
      return im.save('./out.png');
    });
  });

}).call(this);
